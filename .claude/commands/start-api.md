# start-api

Start the Madrid Tracker FastAPI server locally.

```bash
cd madrid-tracker/api && source .venv/bin/activate && uvicorn main:app --reload --port 8000
```

After starting, open: http://localhost:8000/docs
